const {supabase}=require('./_supabase');
module.exports=async(req,res)=>{try{if(req.method!=='POST')return res.status(405).json({error:'POST required'});const auth=req.headers.authorization||'';if(!auth.startsWith('Bearer '))return res.status(401).json({error:'Admin authorization required'});const body=req.body||{};if(!Number(body.bookingId))return res.status(400).json({error:'bookingId required'});const r=await fetch(`${supabase.url}/functions/v1/supplier-orchestrator`,{method:'POST',headers:{'Content-Type':'application/json','apikey':supabase.anon,'Authorization':auth},body:JSON.stringify({bookingId:Number(body.bookingId),traveler:body.traveler||null})});const d=await r.json().catch(()=>({}));return res.status(r.status).json(d)}catch(e){return res.status(500).json({error:e.message||'Supplier fulfillment failed'})}};