const HOST=process.env.AMADEUS_HOST||'https://test.api.amadeus.com';
let cache={token:null,expires:0};
async function accessToken(){
  if(cache.token&&Date.now()<cache.expires-60000)return cache.token;
  const id=process.env.AMADEUS_CLIENT_ID,secret=process.env.AMADEUS_CLIENT_SECRET;
  if(!id||!secret)throw new Error('Amadeus credentials are not configured');
  const r=await fetch(HOST+'/v1/security/oauth2/token',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body:new URLSearchParams({grant_type:'client_credentials',client_id:id,client_secret:secret})});
  const d=await r.json().catch(()=>({}));
  if(!r.ok)throw new Error(d.error_description||'Amadeus authentication failed');
  cache={token:d.access_token,expires:Date.now()+(d.expires_in||1500)*1000};return cache.token;
}
async function request(path,method='GET',body){const t=await accessToken();const r=await fetch(HOST+path,{method,headers:{Authorization:'Bearer '+t,'Content-Type':'application/json'},body:body?JSON.stringify(body):undefined});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d?.errors?.[0]?.detail||d?.errors?.[0]?.title||d?.error_description||`Amadeus HTTP ${r.status}`);return d}
function authOk(req){const expected=process.env.SUPPLIER_ADAPTER_SHARED_SECRET||process.env.SUPPLIER_BOOK_FLIGHT_KEY||'';const got=String(req.headers.authorization||'').replace(/^Bearer\s+/i,'');return Boolean(expected&&got&&expected===got)}
function normalizeTraveler(t,i,contact){const x={id:String(i+1),dateOfBirth:t.dateOfBirth,name:{firstName:String(t.firstName||'').toUpperCase(),lastName:String(t.lastName||'').toUpperCase()},gender:String(t.gender||'').toUpperCase(),contact:{emailAddress:contact.email}};if(contact.phone)x.contact.phones=[{deviceType:'MOBILE',number:String(contact.phone).replace(/\D/g,'')}];if(t.documentNumber){x.documents=[{documentType:String(t.documentType||'PASSPORT').toUpperCase(),number:String(t.documentNumber),expiryDate:t.documentExpiry,nationality:t.nationality,issuanceCountry:t.issuingCountry,validityCountry:t.nationality,holder:true}].map(o=>Object.fromEntries(Object.entries(o).filter(([,v])=>v)))}return x}
module.exports=async(req,res)=>{if(req.method!=='POST')return res.status(405).json({error:'POST required'});if(!authOk(req))return res.status(401).json({error:'Invalid supplier adapter authorization'});try{const b=req.body||{},offer=b.offer?.offer||b.offer?.raw||b.offer,travelers=Array.isArray(b.travelers)?b.travelers:[],contact=b.contact||{};if(!offer||String(b.offer?.mode||b.offer?.inventory_mode||'live')!=='live')return res.status(409).json({error:'LIVE Amadeus flight offer is required'});if(!travelers.length)return res.status(422).json({error:'Traveler details are required before flight booking'});if(!contact.email)return res.status(422).json({error:'Booking contact email is required'});const priced=await request('/v1/shopping/flight-offers/pricing','POST',{data:{type:'flight-offers-pricing',flightOffers:[offer]}});const pricedOffer=priced?.data?.flightOffers?.[0];if(!pricedOffer)return res.status(409).json({error:'Unable to revalidate flight offer'});const before=Number(offer?.price?.grandTotal||offer?.price?.total||0),after=Number(pricedOffer?.price?.grandTotal||pricedOffer?.price?.total||0);const maxIncrease=Number(process.env.FLIGHT_PRICE_INCREASE_TOLERANCE_PERCENT||0);if(before>0&&after>before*(1+maxIncrease/100)+0.01)return res.status(409).json({error:'Flight price increased after revalidation',previousPrice:before,currentPrice:after});const data={type:'flight-order',flightOffers:[pricedOffer],travelers:travelers.map((t,i)=>normalizeTraveler(t,i,contact))};const order=await request('/v1/booking/flight-orders','POST',{data});const o=order?.data||{};const pnr=o.associatedRecords?.[0]?.reference||o.id||null;return res.status(200).json({bookingId:o.id||pnr,confirmationCode:pnr,pnr,provider:'Amadeus',status:'confirmed',price:after,currency:pricedOffer?.price?.currency||'USD'})}catch(e){return res.status(502).json({error:e.message||'Amadeus flight booking failed'})}};